name: Format Canonical Shadow Once

on:
  pull_request:
    paths:
      - '.github/workflows/format-canonical-shadow-once.yml'
      - 'etl/src/ficha_etl/canonical_shadow.py'
      - 'etl/tests/test_canonical_shadow.py'

permissions:
  contents: write

jobs:
  format:
    if: github.event.pull_request.head.repo.full_name == github.repository
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.head_ref }}
          fetch-depth: 0
      - uses: astral-sh/setup-uv@v3
        with:
          enable-cache: true
      - working-directory: etl
        run: |
          uv python install 3.11
          uv sync --extra dev
          uv run ruff format src/ficha_etl/canonical_shadow.py tests/test_canonical_shadow.py
      - name: Commit formatter output
        run: |
          if git diff --quiet -- etl/src/ficha_etl/canonical_shadow.py etl/tests/test_canonical_shadow.py; then
            echo 'already formatted'
            exit 0
          fi
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add etl/src/ficha_etl/canonical_shadow.py etl/tests/test_canonical_shadow.py
          git commit -m 'style(etl): apply repository Ruff formatter'
          git push origin HEAD:${{ github.head_ref }}
